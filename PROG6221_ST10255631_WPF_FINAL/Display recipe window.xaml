<Window x:Class="PROG6221_ST10255631_WPF_FINAL.Display_recipe_window"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PROG6221_ST10255631_WPF_FINAL"
        mc:Ignorable="d"
        Title="Recipe Details" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Recipe Name:" FontWeight="Bold" FontSize="20" Margin="5"/>
        <TextBlock x:Name="RecipeNameTextBlock" Grid.Row="1" Text="{Binding Name}" Margin="5"/>

        <TextBlock Grid.Row="2" Text="Ingredients:" FontWeight="Bold" FontSize="18" Margin="5"/>
        <ListBox x:Name="IngredientsListBox" Grid.Row="3" Margin="5" ItemsSource="{Binding Ingredients}" />

    </Grid>
</Window>